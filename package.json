{
  "name": "aws.dynamo",
  "version": "0.0.1",
  "description": "AWS DynamoDB filter for nsc",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nsc",
    "nsc-filter"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1.4"
  }
}
